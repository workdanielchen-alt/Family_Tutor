diff --git a/vendor/hermes-agent/gateway/platforms/weixin.py b/vendor/hermes-agent/gateway/platforms/weixin.py
index 3c8fa6574..061465cc5 100644
--- a/vendor/hermes-agent/gateway/platforms/weixin.py
+++ b/vendor/hermes-agent/gateway/platforms/weixin.py
@@ -1196,18 +1196,24 @@ class WeixinAdapter(BasePlatformAdapter):
         self._dedup = MessageDeduplicator(ttl_seconds=MESSAGE_DEDUP_TTL_SECONDS)
 
         # ==== HERMES PATCH 2026-05-22: teaching session tracking for DT follow-up ====
-        self._teaching_sessions: dict[str, float] = {}  # sender_id → last_active_ts
-        self._auto_processing_senders: set[str] = set()  # currently auto-processing senders
+        # _is_child: 子网关(学生)走教学流程, 父网关(家长)走 HA agent
+        self._is_child = hermes_home.endswith("/child")
+        # _teaching_sessions: chat_id → {state, started_at, last_active, question_num, total_questions}
+        self._teaching_sessions: dict[str, dict] = {}
+        self._auto_processing_senders: set[str] = set()
         self._teaching_sessions_path = Path(hermes_home) / "weixin" / "teaching_sessions.json"
         self._load_teaching_sessions()
         # ==== END HERMES PATCH ====
 
-        # ==== HERMES PATCH 2026-05-28: HTTP callback server for DT teaching ====
-        self._callback_runner: Optional["web.AppRunner"] = None
-        self._callback_site: Optional["web.TCPSite"] = None
-        self._callback_task: Optional[asyncio.Task] = None
+        # ==== HERMES PATCH 2026-05-28: report notification consumer ====
         self._report_notif_task: Optional[asyncio.Task] = None
-        self._last_tutor_reply: dict[str, str] = {}  # sender_id → tutor_content (kept for backward compat)
+        # ==== END HERMES PATCH ====
+        # ==== HERMES PATCH 2026-05-30: per-learner teaching concurrency guard ====
+        self._running_teachings: set[str] = set()
+        # ==== END HERMES PATCH ====
+        # ==== HERMES PATCH 2026-05-31: P0-1 staged feedback, P1-5/P2-9 improvements ====
+        self._page_acked: set[str] = set()
+        # ==== END HERMES PATCH ====
 
         self._account_id = str(extra.get("account_id") or os.getenv("WEIXIN_ACCOUNT_ID", "")).strip()
         token_str = str(extra.get("token") or os.getenv("WEIXIN_TOKEN", "")).strip()
@@ -1290,8 +1296,7 @@ class WeixinAdapter(BasePlatformAdapter):
         self._send_session = aiohttp.ClientSession(trust_env=True, connector=_make_ssl_connector(), timeout=_no_aiohttp_timeout)
         self._token_store.restore(self._account_id)
         self._poll_task = asyncio.create_task(self._poll_loop(), name="weixin-poll")
-        # ==== HERMES PATCH 2026-05-28: callback server + report notifications ====
-        self._callback_task = asyncio.create_task(self._start_callback_server(), name="weixin-callback")
+        # ==== HERMES PATCH 2026-05-28: report notification consumer ====
         self._report_notif_task = asyncio.create_task(
             self._consume_report_notifications(), name="weixin-report"
         )
@@ -1502,75 +1507,15 @@ class WeixinAdapter(BasePlatformAdapter):
             logger.debug("[%s] LLM lock acquire failed (non-fatal)", self.name)
         # ==== END HERMES PATCH ====
 
-        # ==== HERMES PATCH: Route to DeepTutor teaching flow ====
-        # Three cases:
-        #   1. Has active teaching session (< 2h) → route to tutor_chat
-        #   2. First-time document file (.doc/.docx/.pdf) → auto-initiate teaching
-        #   3. Otherwise → agent
-        _now = time.time()
-        _in_session = (self._teaching_sessions.get(effective_chat_id) or
-                       self._teaching_sessions.get(sender_id))
-        _has_doc = any(p.endswith((".doc", ".docx", ".pdf")) for p in media_paths)
-        _is_image = any(p.endswith((".jpg", ".jpeg", ".png", ".webp")) for p in media_paths)
-        _is_file = _has_doc or _is_image
-
-        # New file always starts fresh teaching (resets old session)
-        if _is_file and _in_session is not None:
-            self._teaching_sessions.pop(effective_chat_id, None)
-            self._teaching_sessions.pop(sender_id, None)
-            self._save_teaching_sessions()
-            _in_session = None
-
-        # Mode switching: chat exits, teach re-enters, doc always starts fresh
-        _reenter_teach = False
-        if text.strip():
-            _chat_signals = {"聊天", "退出", "聊聊", "stop", "exit", "结束", "不做了", "算了", "help", "问个问题"}
-            _teach_signals = {"继续做题", "继续学习", "继续教学", "回来做题", "接着讲", "接着学"}
-            # Exit teaching → agent
-            if _in_session is not None and (
-                text.strip() in _chat_signals or any(text.strip().startswith(kw) for kw in ("聊天", "退出", "聊聊", "问个"))
-            ):
-                self._teaching_sessions.pop(effective_chat_id, None)
-                self._teaching_sessions.pop(sender_id, None)
-                self._save_teaching_sessions()
-                _in_session = None
-                logger.info("[%s] Chat signal, exited teaching session for %s", self.name, effective_chat_id)
-            # Re-enter teaching → tutor_chat (platform preserves old context)
-            if _in_session is None and not _is_file and (
-                text.strip() in _teach_signals or any(text.strip().startswith(kw) for kw in ("继续", "接着"))
-            ):
-                _reenter_teach = True
-                logger.info("[%s] Re-enter teaching signal for %s", self.name, effective_chat_id)
-
-        _should_teach = (_in_session is not None) or (_is_file and _in_session is None) or _reenter_teach
-
-        if _should_teach:
-            if _in_session is not None and _now - _in_session > 7200:
-                # Stale session — clean up
-                self._teaching_sessions.pop(effective_chat_id, None)
-                self._teaching_sessions.pop(sender_id, None)
-                self._save_teaching_sessions()
-            else:
-                platform_url = os.getenv("PLATFORM_API_URL", "http://platform:8100")
-                # Use text if present, otherwise describe the file
-                _msg = text.strip() if text.strip() else f"请分析这份{'、'.join(media_paths)}"
-
-                # Quick acknowledgment + fire-and-forget teaching in background
-                if _is_file:
-                    try:
-                        await self.send(content=" 正在分析试卷，请稍候...", chat_id=effective_chat_id)
-                    except Exception:
-                        pass
-
-                # IMMEDIATELY set teaching session BEFORE the async flow
-                # so any follow-up text from the student is routed to teaching
-                # rather than falling through to the general HA LLM (race fix).
-                self._teaching_sessions[effective_chat_id] = time.time()
-                self._save_teaching_sessions()
-                asyncio.create_task(self._run_teaching_flow(
-                    effective_chat_id, _msg, platform_url, _is_file, media_paths, _is_image,
-                ))
-                return
+        # ==== HERMES PATCH: Dual-gateway routing + three-state teaching ====
+        if self._is_child:
+            # 子网关 → 完整教学路由（三态会话引擎）
+            await self._route_child_message(event, text, media_paths, effective_chat_id, sender_id)
+            return
+        else:
+            # 父网关 → 简化路由（不教学）
+            await self._route_parent_message(event, text, media_paths, effective_chat_id)
+            return
         # ==== END HERMES PATCH ====
 
         await self.handle_message(event)
@@ -2233,40 +2178,9 @@ class WeixinAdapter(BasePlatformAdapter):
             await asyncio.sleep(3)
     # ==== END HERMES PATCH ====
 
-    # ==== HERMES PATCH 2026-05-28: HTTP callback server for DT teaching ====
-    async def _start_callback_server(self) -> None:
-        """Start a minimal aiohttp server for receiving teaching callbacks from platform."""
-        _is_child = os.environ.get("HERMES_HOME", "").endswith("/child")
-        _port = int(os.getenv("WEIXIN_CALLBACK_PORT", "8006" if _is_child else "8005"))
-        _host = os.getenv("WEIXIN_CALLBACK_HOST", "0.0.0.0")
-        try:
-            app = web.Application()
-            app.router.add_post("/api/tutor/callback", self._handle_tutor_callback)
-            self._callback_runner = web.AppRunner(app)
-            await self._callback_runner.setup()
-            self._callback_site = web.TCPSite(self._callback_runner, _host, _port)
-            await self._callback_site.start()
-            logger.info("[%s] Tutor callback server started on %s:%d", self.name, _host, _port)
-        except Exception as exc:
-            logger.warning("[%s] Tutor callback server failed to start: %s", self.name, exc)
-
-    async def _handle_tutor_callback(self, request: "web.Request") -> "web.Response":
-        """Receive teaching response from platform and deliver via WeChat."""
-        try:
-            body = await request.json()
-            chat_id = body.get("chat_id", "")
-            content = body.get("content", "")
-            if chat_id and content:
-                await self.send(content=content, chat_id=chat_id)
-                logger.info("[%s] Tutor callback delivered to %s (%d chars)", self.name, chat_id, len(content))
-                return web.Response(status=200, text="ok")
-            return web.Response(status=400, text="missing chat_id or content")
-        except json.JSONDecodeError:
-            return web.Response(status=400, text="invalid JSON")
-        except Exception as exc:
-            logger.warning("[%s] Tutor callback error: %s", self.name, exc)
-            return web.Response(status=500, text="internal error")
-    # ==== END HERMES PATCH ====
-
     # ── Teaching flow (consolidated, no HTTP callbacks) ────────────
     async def _run_teaching_flow(
         self,
@@ -2279,16 +2193,34 @@ class WeixinAdapter(BasePlatformAdapter):
     ) -> None:
         """Fire-and-forget teaching flow."""
         if effective_chat_id in self._running_teachings:
-            logger.debug("[%s] Teaching already running for %s, skipping", self.name, effective_chat_id)
+            logger.debug(
+                "[%s] Teaching already running for %s, skipping", self.name, effective_chat_id
+            )
             try:
                 await self.send(content=" 正在处理，请稍候...", chat_id=effective_chat_id)
             except Exception:
                 pass
             return
         self._running_teachings.add(effective_chat_id)
-        _start = time.time()
         try:
+            # P0-1: Send immediate "reading" acknowledgment for file path
+            if is_new_file and media_paths:
+                try:
+                    _ack = "📎 收到！正在读题"
+                    if len(media_paths) > 1:
+                        _ack += f"（共{len(media_paths)}页）"
+                    _ack += "..."
+                    await self.send(content=_ack, chat_id=effective_chat_id)
+                except Exception:
+                    pass
+            elif is_new_file:
+                try:
+                    await self.send(
+                        content="📎 收到！正在读题...", chat_id=effective_chat_id
+                    )
+                except Exception:
+                    pass
+
             async with aiohttp.ClientSession(timeout=aiohttp.ClientTimeout(total=180)) as session:
                 # ── File path: extract text from ALL pages, then teach ──
                 # Handles images (OCR), Office docs, PDFs — any file type.
@@ -2299,38 +2231,65 @@ class WeixinAdapter(BasePlatformAdapter):
                         _filename = os.path.basename(_fp)
                         try:
                             with open(_fp, "rb") as f:
-                                form = aiohttp.FormData()
-                                form.add_field("file", f, filename=_filename)
-                                form.add_field("learner_id", effective_chat_id)
-                                form.add_field("kb_name", "tutoring")
-                                form.add_field("suppress_auto_teach", "1")
-                                resp = await session.post(
-                                    f"{platform_url}/api/process/file",
-                                    data=form,
-                                )
+                                _form = aiohttp.FormData()
+                                _form.add_field("file", f, filename=_filename)
+                                _form.add_field("learner_id", effective_chat_id)
+                                _form.add_field("kb_name", "tutoring")
+                                _form.add_field("suppress_auto_teach", "1")
+                                resp = await session.post(
+                                    f"{platform_url}/api/process/file",
+                                    data=_form,
+                                )
                             if resp.status == 200:
                                 data = await resp.json()
                                 if data.get("ok") and data.get("content"):
                                     _txt = data["content"].strip()
                                     if _txt:
-                                        _page_texts.append(f"--- 第{_idx+1}页 ---\n{_txt}")
+                                        _page_texts.append(
+                                            f"--- 第{_idx+1}页 ---\n{_txt}"
+                                        )
                                         _page_count += 1
                                         logger.info(
                                             "[%s] OCR page %d: %d chars for %s",
-                                            self.name, _idx + 1, len(_txt), effective_chat_id,
+                                            self.name,
+                                            _idx + 1,
+                                            len(_txt),
+                                            effective_chat_id,
+                                        )
+                                        # P0-1: Per-page progress
+                                        if (
+                                            len(media_paths) > 1
+                                            and _page_count < len(media_paths)
+                                        ):
+                                            try:
+                                                await self.send(
+                                                    content=f"📄 第{_page_count}/{len(media_paths)}页已看清...",
+                                                    chat_id=effective_chat_id,
+                                                )
+                                            except Exception:
+                                                pass
                         except Exception as exc:
                             logger.warning(
                                 "[%s] OCR failed for page %d (%s): %s",
-                                self.name, _idx + 1, _filename, exc,
+                                self.name, _idx + 1, _filename, exc
                             )
                     if _page_texts:
                         _teach_context = "\n\n".join(_page_texts)
 
-                # ── Send page detection progress (only on new files) ──
-                if _page_count > 1:
-                    _total_q = len(re.findall(
-                        r"(?:第|^)\s*(\d+)\s*[.、．）\)]\s|[（(]\s*\d+\s*[）)]\s*|[」】]?\s*(?:选|填|计|证|解|化简|求|判|作|画|已知|若|设|如|在|将|把|当|对于|如图|下列|根据|按照|观察|思考|探究|阅读|理解)\S{0,4}",
-                        _teach_context[:3000],
-                    ))
+                # P0-1: Send "preparing questions" after OCR completes
+                if _page_count > 0:
+                    try:
+                        _page_msg = f"📄 已识别 {_page_count} 页"
+                        if len(media_paths) > 1:
+                            _page_msg += "，正在出题..."
+                        else:
+                            _page_msg += "，正在出题..."
+                        await self.send(content=_page_msg, chat_id=effective_chat_id)
+                    except Exception:
+                        pass
+                    _cur = self._teaching_sessions.get(effective_chat_id)
+                    if isinstance(_cur, dict):
+                        _cur["total_questions"] = 0
+                        self._save_teaching_sessions()
 
                 # ── Build the teaching message ──
                 if _teach_context:
@@ -2394,12 +2353,21 @@ class WeixinAdapter(BasePlatformAdapter):
             pass
 
     # ── 子网关路由：三态会话教学引擎 ────────────────────────────
+    # P1-5: Extended pause/resume signals for child-friendly expressions
     _PAUSE_SIGNALS = {"累了", "休息一下", "聊会天", "想聊天",
                       "不学了", "先不学了", "休息", "歇会",
-                      "先休息", "不想学了", "聊聊天", "说会话"}
+                      "先休息", "不想学了", "聊聊天", "说会话",
+                      "等一下", "妈妈叫我", "吃饭了", "要走了",
+                      "下次再做", "先不做了", "先放这", "等会儿",
+                      "先去", "有事", "待会", "先不学了",
+                      "不想做了", "太累了", "困了", "歇会",
+                      "离开一下"}
     _RESUME_SIGNALS = {"继续", "接着", "回来", "好了",
                        "继续做题", "接着学", "开始学习",
-                       "回来做题", "继续学习", "开始做题"}
+                       "回来做题", "继续学习", "开始做题",
+                       "回来了", "来了", "做完了", "我好了",
+                       "我又来了", "继续吧", "开始吧", "来吧",
+                       "我回来了", "好了继续"}
 
     async def _route_child_message(
         self, event, text: str, media_paths: list,
@@ -2412,14 +2380,18 @@ class WeixinAdapter(BasePlatformAdapter):
         # ── 1. 新文件 → 总是重置教学 ──
         if _is_file:
             self._teaching_sessions[chat_id] = self._new_session("active")
+            self._teaching_sessions[chat_id]["acknowledged"] = False
             self._save_teaching_sessions()
             platform_url = os.getenv("PLATFORM_API_URL", "http://platform:8100")
-            # Quick acknowledgment so the student knows the file was received
-            try:
-                await self.send(content="📎 收到，正在处理...", chat_id=chat_id)
-            except Exception:
-                pass
-            asyncio.create_task(self._run_teaching_flow(
+            # P0-1: Immediate acknowledgment in _route_child_message
+            try:
+                _ack = "📎 收到！正在读题"
+                if len(media_paths) > 1:
+                    _ack += f"（共{len(media_paths)}页）"
+                _ack += "..."
+                await self.send(content=_ack, chat_id=chat_id)
+            except Exception:
+                pass            asyncio.create_task(self._run_teaching_flow(
                 chat_id, "", platform_url, True, media_paths, False,
             ))
             return
@@ -2440,6 +2412,29 @@ class WeixinAdapter(BasePlatformAdapter):
                     pass
                 return
 
+            # P2-9: Idle detection on student return
+            _idle_secs = _now - _session.get("last_active", _now)
+            if _idle_secs > 600 and _session.get("question_num", 0) > 0:
+                _session["state"] = "paused"
+                _session["last_active"] = _now
+                self._save_teaching_sessions()
+                try:
+                    await self.send(
+                        content=" 你很久没回复了，先休息一下~ 说「继续做题」回来继续！",
+                        chat_id=chat_id,
+                    )
+                except Exception:
+                    pass
+                return
+            elif _idle_secs > 300 and _session.get("question_num", 0) > 0:
+                try:
+                    await self.send(
+                        content=" 还在吗？需要提示的话告诉老师哦~",
+                        chat_id=chat_id,
+                    )
+                except Exception:
+                    pass
+
             # 其他所有消息（答案、问问题、请求讲解、闲聊）→ tutor_chat
             _msg = text.strip() if text.strip() else "继续"
             platform_url = os.getenv("PLATFORM_API_URL", "http://platform:8100")
