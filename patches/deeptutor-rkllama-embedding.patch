--- a/deeptutor/services/config/provider_runtime.py
+++ b/deeptutor/services/config/provider_runtime.py
@@ -128,6 +128,15 @@
         keywords=("vllm", "lmstudio"),
         is_local=True,
     ),
+    "rkllama": EmbeddingProviderSpec(
+        label="RKLLM Local",
+        mode="local",
+        default_api_base="",
+        keywords=("rkllama", "rkllm", "bge-m3"),
+        is_local=True,
+        default_model="bge-m3",
+        default_dim=1024,
+    ),
     "siliconflow": EmbeddingProviderSpec(
         label="SiliconFlow",
         adapter="openai_compat",
