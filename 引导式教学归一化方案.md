# 引导式教学归一化方案（v2）

## 核心原则

两个入口，一条路径，来源 (source) 是唯一区别。

```
家长微信 ── OCR（Platform）──┐
                             ├── POST /api/teach/start
孩子 WebUI ── OCR（Platform）──┘       │
  (REST, 不走 WebSocket)               ├── TeachSession { source: "wechat" | "webui" }
                                       │
                                       └── _tutor_chat_core (mode=guide)
```

## 关键发现：OCR 必须归一化到 Platform

调查确认：

| 处理能力 | DeepTutor | Platform |
|---------|-----------|----------|
| PDF/DOCX 文本提取 | ✅ 自有的 `document_extractor.py` | ✅ `_handle_inbound_file` |
| **图片 OCR**（rkllama + OpenCV） | **❌ 没有** | ✅ 完整管道 |
| 图片处理方式 | base64 直接丢给多模态 LLM | OCR → 结构化文本 |
| 旧版 .doc | ❌ 不支持 | ✅ antiword |

**结论：** DT 没有图片 OCR 能力，OCR 必须在 Platform 完成归一化。

这也意味着 WebUI 发文件不再走 WebSocket → DeepTutor，而是走 REST → Platform。

## 归一化后的体验

### 家长微信发题

```
① 家长发文件 → POST /api/process/file（仅 OCR 不做教学）
   → 返回 ocr_text
② POST /api/teach/start { ocr_text, source_file, learner_id }
   → Platform 创建 TeachSession(source="wechat")
   → _tutor_chat_core(context=ocr_text, mode="guide")
   → 返回第一题
③ 微信回复"已收到，让孩子在电脑上看看吧"
④ 孩子打开 WebUI → 看到"📚 家长发来的试卷"待办
⑤ 点击 → 进入教学（同 WebUI 自上传入口以下流程）
```

### 孩子 WebUI 上传文件

```
① 孩子上传文件 → POST /api/teach/start { file_base64, filename, learner_id }
   → Platform → _handle_inbound_file → OCR 文本
   → 创建 TeachSession(source="webui")
   → _tutor_chat_core(context=ocr_text, mode="guide")
   → 返回第一题
② 聊天页显示第一题
③ 孩子输入答案 → POST /api/teach/continue { teach_session_id, message }
   → _tutor_chat_core(message, mode="guide")
   → 返回评改结果 + 下一题（或 done）
④ 逐题完成 ✓
```

### 核心数据流

```
                 POST /api/teach/start
                 ┌──────────────────┐
微信 ocr_text ──→│  文件分类 + OCR  │←── WebUI file_base64
                 │  _handle_inbound │
                 │  _file           │
                 └────────┬─────────┘
                          │ ocr_text
                          ▼
                 创建 TeachSession
                   { source, ocr_text, ... }
                          │
                          ▼
                  _tutor_chat_core
                  (context=ocr_text, mode="guide")
                          │
                          ▼
                  回复 { first_question, total_questions }
                          │
                          ▼
                  POST /api/teach/continue
                  (逐题，归一化后两个入口无区别)
```

## API

### `POST /api/teach/start`

请求（二选一）：
```json
// 微信路径（已有 OCR 文本）
{ "ocr_text": "...", "learner_id": "...", "source_file": "..." }
// WebUI 路径（原始文件）
{ "file_base64": "/9j/4AAQ...", "filename": "test.jpg", "learner_id": "..." }
```

返回：
```json
{
  "ok": true,
  "teach_session_id": "ts_xxx",
  "first_question": "第1题\n\n【题目】...",
  "total_questions": 20,
  "source": "wechat"
}
```

### `POST /api/teach/continue`

请求：
```json
{ "teach_session_id": "ts_xxx", "message": "我选A", "learner_id": "..." }
```

返回：
```json
{
  "ok": true,
  "reply": "✅ 正确！...\n\n═══════════\n正确答案：A\n═══════════\n\n第2题\n\n【题目】...",
  "current": 2,
  "total_questions": 20,
  "done": false
}
```

### `GET /api/teach/pending/{learner_id}`

返回：
```json
{
  "ok": true,
  "sessions": [
    {
      "session_id": "ts_xxx",
      "source": "wechat",
      "total_questions": 20,
      "current_question": 0,
      "status": "pending",
      "created_at": 1717200000
    }
  ]
}
```

### `GET /api/teach/session/{session_id}`

返回单个 session 详细信息（含 first_question）。

## TeachSession 存储

位置：`/data/teach_sessions/ts_*.json`

```json
{
  "session_id": "ts_xxx",
  "learner_id": "o9cq802xxx@im.wechat",
  "status": "active",
  "source": "wechat",
  "ocr_text": "全文OCR文本...",
  "source_file": "初三化学试题.doc",
  "total_questions": 20,
  "current_question": 1,
  "first_question": "第1题\n\n【题目】...",
  "created_at": 1717200000,
  "expires_at": 1717372800
}
```

## 文件变更总表

| 文件 | 操作 | 说明 |
|------|------|------|
| `tutor_platform/teach_session.py` | **新增** | TeachSession 数据模型 + JSON 文件存储 |
| `docker/platform/provider_api.py` | **修改** | 新增 4 个 teach 端点；删除 `_generate_quiz_from_ocr`、`create_quiz`、QuizSession 相关 |
| `web/lib/platform-api.ts` | **修改** | 新增 `startTeach()`, `continueTeach()`, `fetchPendingTeach()`, `fetchTeachSession()` |
| `web/app/(app)/chat/[[...sessionId]]/page.tsx` | **修改** | 文件上传 → REST /teach/start；回答 → REST /teach/continue；展示待办 |
| `vendor/.../weixin.py` | **patch** | 去掉 create_quiz 和卡片链接；改为 POST /api/teach/start |
| `tutor_platform/quiz_session.py` | **删除** | 不再需要 |
| `patches/wechat-quiz-card-link.patch` | **删除** | 已废弃 |
