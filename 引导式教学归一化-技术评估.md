# 技术可行性 & 效率评估

## 1. 可行性质疑清单

### Q1: Platform 的 OCR 能否同时服务两个入口？

**是。** `_handle_inbound_file` 已经封装在 `provider_api.py`，支持：

| 文件类型 | 处理方式 | 速度 |
|---------|---------|------|
| 图片 (.jpg/.png) | OpenCV 预处理 → rkllama OCR | ~2-8s/页（热启动） |
| PDF（文本层） | pymupdf 直接提取 | <1s |
| PDF（扫描件） | 渲染 → OpenCV → rkllama OCR | ~3-10s/页 |
| Office (.docx/.xlsx/.pptx) | markitdown 提取 | <1s |
| 旧版 .doc | antiword 提取 | <1s |
| 纯文本 | 直接读取 | 即时 |

微信路径已在使用这条管道。WebUI 路径加一个 base64 解析步骤（`api_extract_file` 已有先例），后续进同一管道。

**结论：✅ 可行**

### Q2: `_tutor_chat_core` 能否被两个入口同时调用？

**是。** 已有先例：

- 微信子网关（孩子发题）：`weixin.py` → `POST /api/tutor/chat` → `_tutor_chat_core`
- 微信家长发题（改造后）：`POST /api/teach/start` → `_tutor_chat_core`
- WebUI 上传文件（改造后）：`POST /api/teach/start` → `_tutor_chat_core`

`_tutor_chat_core` 通过 `learner_id` 隔离上下文（`_last_tutor_context[learner_id]`），多入口共享无冲突。

**结论：✅ 可行**

### Q3: `_last_tutor_context` 内存缓存丢失怎么办？

`_last_tutor_context` 是进程内 dict，Platform 重启后丢失。

**缓解：** `TeachSession.ocr_text` 存了全文。`POST /api/teach/continue` 检测到上下文丢失时（通过 `_last_tutor_context.get(learner_id)` 是否为空），可以从 TeachSession 重新注入：
```python
if not _last_tutor_context.get(learner_id):
    # 上下文丢失，从 TeachSession 恢复
    _last_tutor_context[learner_id] = session.ocr_text
```

这是增强健壮性的可选优化。现有微信教学流程同样有这个问题，实践中很少发生。

**结论：✅ 可行（现有流程序已接受此风险）**

### Q4: 微信两个路径（孩子直接教学 vs 家长发题到 WebUI）会不会冲突？

**不会。** 两个路径走同一个函数（`_run_teaching_flow`）但触发条件不同：

| 路径 | 触发 | learner_id | 行为 |
|------|------|-----------|------|
| 孩子微信发题 | 子网关收到文件 | 孩子的微信 ID | OCR → tutor_chat → 微信内对话 |
| 家长微信发题 | 父网关收到文件 | 孩子的微信 ID | OCR → `/api/teach/start` → TeachSession |

两个路径使用不同的 `_running_teachings` guard（子网关用子网关的，父网关用父网关的），不会互相阻塞。

**结论：✅ 可行**

### Q5: WebUI 同时有 WebSocket 聊天 + REST 教学，模式切换是否干净？

核心设计：

```
WebUI Chat 组件：

  状态: teachSessionId | null

  sendMessage():
    if teachSessionId:
      → POST /api/teach/continue { teach_session_id, message }
      → 显示回复
      → if done: clear teachSessionId
    else if file_attached:
      → POST /api/teach/start { file_base64, ... }
      → set teachSessionId
      → 显示第一题
    else:
      → WebSocket (原有逻辑)
```

用一个 state 隔离两种模式。教学模式中文件上传按钮可禁用或重新开始教学。

**结论：✅ 可行，模式切换逻辑清晰**

### Q6: 需要改 vendor/deeptutor 代码吗？

**不需要。** 所有改动都在 Platform（自有代码）和 WebUI（自有代码）。WeChat weixin.py 在 vendor/hermes-agent，有 patch 机制。

受影响 vendor：

| Vendor | 文件 | 操作 | 追踪方式 |
|--------|------|------|---------|
| hermes-agent | `weixin.py` | 修改 | `patches/wechat-teach-flow.patch`（新建） |
| deeptutor | 无 | — | — |

**结论：✅ 不改 DT 核心**

---

## 2. 效率评估

### 2.1 延迟分解

以 WebUI 上传一张试卷照片为例：

| 步骤 | 时间 | 说明 |
|------|------|------|
| ① base64 解码 + 写临时文件 | ~0.1s | 内存操作 |
| ② OpenCV 预处理 | ~0.2s | 缩放到 OCR 适合尺寸 |
| ③ rkllama OCR | ~2-5s | 主要耗时，NPU 推理 |
| ④ `_tutor_chat_core` 首次调用 | ~5-15s | LLM 生成第一题 |
| **总耗时** | **~7-20s** | 用户预期内（文件上传->出题） |

后续逐题（`/api/teach/continue`）：

| 步骤 | 时间 | 说明 |
|------|------|------|
| ① `_tutor_chat_core` 评改+出题 | ~3-10s | LLM 生成 |
| **总耗时** | **~3-10s** | 同微信教学体验 |

### 2.2 对比当前路径

**当前 WebUI 文件上传（WS → DeepTutor）：**
```
WebUI → WS → DeepTutor → document_extractor → LLM chat
```
- 图片：无 OCR，base64 直接给 LLM。LLM 需要从图片中"看"出题目 → 慢且不可靠
- 文档：文本提取 <0.5s，但走的是通用 chat 能力，不是引导式教学

**改造后：**
```
WebUI → REST → Platform → OCR → text → _tutor_chat_core → 引导式教学
```
- 图片：OCR 2-5s + 教学 5-15s，总 7-20s。但结果是高质量的引导式教学
- 文档：提取 <0.5s + 教学 5-15s，总 ~6-15s

**对比结论：** 多了 OCR 的 2-5s，但换取的是：
1. 图片 OCR 的可靠性远高于多模态 LLM 直接"看图识字"
2. 引导式教学代替通用 chat，学习效果有本质提升

### 2.3 资源消耗

| 资源 | 消耗 | 说明 |
|------|------|------|
| CPU（OCR 预处理） | 单次 ~0.2s，几乎可忽略 | OpenCV 预处理 |
| NPU/GPU（rkllama） | 单次 ~2-5s 推理 | OCR 模型推理，热启动后快 |
| LLM API | 每轮 1 次调用 | `_tutor_chat_core` 每个问题 1 次 |
| 内存 | TeachSession <10KB/条 | JSON 文件存储，无 DB |
| 磁盘 | 按文件数线性 | 自动过期清理 |

对于教师/家长发题场景（非高频），资源消耗在可接受范围内。

---

## 3. 风险矩阵

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| rkllama OCR 失败/超时 | 低 | OCR 错误，教学无法开始 | Platform 已有错误处理，返回清晰错误信息 |
| LLM 生成超时 | 中 | 教学响应慢 | `_tutor_chat_core` 有 90s 超时，降级返回 |
| WebUI 模式切换 bug | 中 | 消息发错路径 | 严格隔离 state，单元测试 |
| 微信家长教学和微信孩子教学混淆 | 低 | 上下文串 | 通过 learner_id + source 隔离 |
| WebUI 同时上传文件+聊天 | 低 | 混淆 | 文件上传即进入 teach 模式，禁止并行 WS 聊天 |
| 旧 QuizSession 数据残留 | 低 | 无影响 | 不删除，也不创建新的 |

---

## 4. 最终结论

**完全可行。** 无阻塞性障碍。

- **架构**：OCR 归一化到 Platform 是唯一正确选择（DT 没有图片 OCR）
- **效率**：OCR 增加 2-8s 延迟，但换来可靠的教学文本提取
- **安全性**：不改 vendor/deeptutor，weixin.py 有 patch 机制
- **可维护性**：新增模块 `teach_session.py` 独立且简单
- **可测试性**：每个 Phase 独立可验证
